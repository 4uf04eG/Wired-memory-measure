# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MSS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  # Pods for MSS
  pod 'Charts'
  pod 'TinyConstraints'
  pod 'PythonKit', git: 'https://github.com/pvieito/PythonKit.git', branch: 'master'
		
end
